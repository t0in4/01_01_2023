import java.util.Scanner;
public class Main {
	public static void class main(String[] args) {
		Scanner scanner = new Scanner(System.in);
		// дописать код
		int size = ввод размера массива;
		// объявили массив с заданным размером
		int[] myArray = new int[size]; // int[size] переменная которую инициализировали в предыдущей строке
        	for (int i = 0; i < size; i++) {
            		myArray[i] = scanner.nextInt();
        	}
		ввод числа сравнения;
		декларация переменной для суммы;
		for (int i = 0; i < myArray.length; i++) {
			if (i < (меньше чем число сравнения) {
				(переменная суммы)++
			}
		}
		System.out.println( переменная суммы );
	}
}
